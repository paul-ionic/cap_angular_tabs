import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cap_angular_tabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
